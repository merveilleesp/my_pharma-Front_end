/*import 'package:flutter/material.dart';
import 'medicaments.dart';
import 'panier.dart'; // Assurez-vous d'importer votre fichier panier.dart


class ParentWidget extends StatelessWidget {
  ParentWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) { // Exemple de panier

    return Scaffold(
      body: PanierPage(panier: panier), // Passez la liste panier à la classe Panier
    );
  }
}*/