/*import 'package:flutter/material.dart';
import 'package:flutter/gestures.dart';

class ResetPasswordScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Réinitialisation du mot de passe"),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            Text(
              "Entrez votre adresse e-mail pour réinitialiser le mot de passe",
              textAlign: TextAlign.center,
            ),
            SizedBox(height: 20),
            TextField(
              decoration: InputDecoration(
                hintText: "Adresse e-mail",
                border: OutlineInputBorder(),
              ),
            ),
            SizedBox(height: 20),
            ElevatedButton(
              onPressed: () {
                // Logique pour envoyer un e-mail de réinitialisation
                // Ici, vous pouvez implémenter l'envoi d'un e-mail de réinitialisation
                // ou une autre méthode appropriée pour gérer cette action.
                print("Envoyer e-mail de réinitialisation");
              },
              child: Text("Réinitialiser le mot de passe"),
            ),
          ],
        ),
      ),
    );
  }
}*/
